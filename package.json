{
  "name": "ecofossil",
  "version": "1.0.0",
  "description": "Projeto EcoFossil - CRUD com JSON Server",
  "main": "index.js",
  "scripts": {
    "start:json": "json-server --watch db/db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
